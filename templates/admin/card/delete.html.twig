{% extends 'layouts/admin.html.twig' %}

{% block content %}

    <div class="form-group">
        <h3 class="text-center"> Cartes à supprimer</h3>
        <select multiple class="form-control" id="cards">
            {% for card in cards %}
                <option> {{ card.code }}</option>
            {% endfor %}
        </select>
    </div>
    <button type="submit" class="btn btn-primary mb-2">Supprimer</button>
{% endblock %}

{% block page_title %}
 Suppression de cartes
{% endblock %}

